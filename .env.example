# Memory Bank Configuration

# Required: Directory where photos will be stored on disk
PHOTO_STORAGE_PATH=/path/to/photos

# Optional: Path to SQLite database file (default: ./photos.db)
DATABASE_PATH=/path/to/photos.db

# Optional: Server port (default: 8080)
PORT=8080

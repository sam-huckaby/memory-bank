# Memory Bank Configuration

# Required: Directory where photos will be stored on disk
PHOTO_STORAGE_PATH=/path/to/photos

# Optional: Path to SQLite database file (default: ./photos.db)
DATABASE_PATH=/path/to/photos.db

# Optional: Directory containing database migration SQL files (default: ./migrations)
MIGRATION_DIR=./migrations

# Optional: Server port (default: 8080)
PORT=8080

# Optional: Network interface to bind to (default: 0.0.0.0)
# 0.0.0.0 = all network interfaces (accessible from other machines)
# 127.0.0.1 = localhost only (accessible only from this machine)
INTERFACE=0.0.0.0

# Optional: CORS allowed origins (default: * allows all origins)
# Use comma-separated list for multiple origins
# Examples:
#   CORS_ALLOWED_ORIGINS=*                                           # Allow all (default, suitable for home networks)
#   CORS_ALLOWED_ORIGINS=http://localhost:3000                       # Single origin (web app)
#   CORS_ALLOWED_ORIGINS=http://localhost:3000,http://192.168.1.5:3000  # Multiple origins
# Note: Android apps can use local network IP (e.g., http://192.168.1.100:8080)
CORS_ALLOWED_ORIGINS=*
